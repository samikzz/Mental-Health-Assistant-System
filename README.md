# Mental-Health-Assistant-System
Mental Health Assistance System – A multimodal system for emotion detection from text and voice using TF-IDF + Random Forest and MFCC + SVM, combined via a voting classifier, achieving 83% (text) and 78% (voice) accuracy, with an LLM-based chatbot providing personalized support
